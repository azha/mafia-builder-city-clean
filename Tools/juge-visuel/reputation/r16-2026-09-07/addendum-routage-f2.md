# Addendum ㊲ r16 — routage de f2 (2026-09-07 ~09:35), consigné tel quel (décisions de f2, pas de l'user, pas du juge)

| écart r16 | décision f2 | destinataire |
|---|---|---|
| M1 ascenseur posé SUR le contenu (1920) — gouttière libre de 25 px à 27 px à droite | correctif | correcteur |
| M2 panneau −89 px, carte +10 px hors du panneau — deux exigences mesurées (tuiles 465 · panneau 717 vs 678, déficit 39) | **f2 retire la colonne de tuiles à 465 px** : « le joueur lit une RELATION (dedans/dehors), pas une cote » ⇒ le déficit se prend sur la LARGEUR, la carte reste DANS le panneau | correcteur |
| R2 250 px de bande morte au-dessus du cadre à 2400 (le témoin ⑱ du même commit remplit la zone à 100 %) | **retiré des arbitrages** : deux écrans du même shell, même commit, même zone — l'un remplit, l'autre pas ⇒ incohérence, pas design ; le témoin prouve que le shell en est capable | correcteur (hauteur de cadre) |
| M3 visage hors chevelure (récurrent r15 → r16) | — | blender / DA |
| R5 sous-libellé de tuile à 3,71:1 DANS la référence | — | blender / DA |
| R4 `HEAT` / `$ 24 850` = maquette en retard | — | blender |
| R1 halo (déclaration VÉRIFIÉE : existe, centré, sans soudure, portée 3 ≤ 6) · R3 police · R6 ronds du dock | — | user (halo avec TD-685) |

Règle retenue par f2 : *une incompatibilité déclarée sans dire laquelle des deux branches on retire n'est pas un arbitrage, c'est un renvoi* ;
*un témoin transforme « c'est peut-être voulu » en « l'un le fait, l'autre pas »* ; *un écart CONSTANT non expliqué (+18 px `[CADRE-B3]`) est
un repère qui diffère, pas un défaut — ne pas classer tant que le retrait n'est pas tenu*. GO r17 attendu quand M1 et M2 sont posés ; le halo n'y sera plus.
